---
// pages/test-carousel.astro
import Layout from '../layouts/BlogPost.astro';
import ReelCarousel from '../components/ReelCarousel.astro';

const products = Array.from({ length: 6 }, (_, i) => ({
  name: `Elegant Gold Ring #${i + 1}`,
  imageUrl: '/testImage.png',
  price: Math.floor(Math.random() * 10000),
  currency: 'INR',
  isSoldOut: false,
}));
---

<Layout
  title="Component Test – ProductCard Carousel"
  description="Testing the SimpleProductCarousel component layout and behavior"
  pubDate={new Date('August 08 2021')}
  heroImage="/blog-placeholder-about.jpg"
>
  <ReelCarousel products={products} />
</Layout>
